name: interfaz_usuario
description: Un paquete de UI reusable que contiene widgets y temas para la aplicación del restaurante.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.0.0
  google_fonts: ^6.1.0 
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
